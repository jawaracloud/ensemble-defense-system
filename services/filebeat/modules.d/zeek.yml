- module: zeek
  capture_loss:
    enabled: true
    paths: ["${LOG_DIR}/zeek/capture_loss.log"]
  connection:
    enabled: true
    paths: ["${LOG_DIR}/zeek/conn.log"]
  dhcp:
    enabled: true
    paths: ["${LOG_DIR}/zeek/dhcp.log"]
  dns:
    enabled: true
    paths: ["${LOG_DIR}/zeek/dns.log"]
  files:
    enabled: true
    paths: ["${LOG_DIR}/zeek/files.log"]
  http:
    enabled: true
    paths: ["${LOG_DIR}/zeek/http.log"]
  notice:
    enabled: true
    paths: ["${LOG_DIR}/zeek/notice.log"]
  ntp:
    enabled: true
    paths: ["${LOG_DIR}/zeek/ntp.log"]
  sip:
    enabled: true
    paths: ["${LOG_DIR}/zeek/sip.log"]
  ssh:
    enabled: true
    paths: ["${LOG_DIR}/zeek/ssh.log"]
  ssl:
    enabled: true
    paths: ["${LOG_DIR}/zeek/ssl.log"]
  stats:
    enabled: true
    paths: ["${LOG_DIR}/zeek/stats.log"]
  weird:
    enabled: true
    paths: ["${LOG_DIR}/zeek/weird.log"]
  x509:
    enabled: true
    paths: ["${LOG_DIR}/zeek/x509.log"]
  packet_filter:
    enabled: true
    paths: ["${LOG_DIR}/zeek/packet_filter.log"]
